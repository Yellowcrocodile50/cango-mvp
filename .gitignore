# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# local dev tools
.mcp.json
pw-open.mjs

# typescript
*.tsbuildinfo
next-env.d.ts

# local reference assets (작업용 참고 이미지 등 — 레포에 커밋 안 함)
/환산 예시.png
/자료보관_선행학습영향평가/

# Playwright MCP 산출물 (스냅샷·콘솔 로그)
.playwright-mcp/

# 내신 계산기 검수용 산출물 (naeshinCutoffs.ts에서 생성 — 원본이 아니라 파생물이라 커밋 안 함)
/naeshin_v3_전체리스트.md
/naeshin_v2_검수프롬프트.md

# 진로 탐구 검수용 산출물 (careerDepartments.ts에서 생성 — 파생물이라 커밋 안 함)
/career_검수프롬프트.md
/career_검수_배치*.md
/career_검수2_*.md
/career_자격검수_*.md

# 글바치 원본 HTML (진로 탐구 이식 원본 — 외부 프로젝트라 이 레포에 커밋하지 않음)
/글바치.html
